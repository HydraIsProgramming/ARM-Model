# =============================================================================
# RL Arm Motion — Runtime Dependencies
# =============================================================================
# Project:    CP493 Directed Research — Fischer et al. (2021) RL methodology
#             implementation on a 2-DOF robotic arm
# Author:     Ranjot Sandhu, Wilfrid Laurier University
# Python:     3.10+ (tested on Python 3.10 with all packages below)
# Install:    pip install -r requirements.txt
# =============================================================================
# Lower bounds are the versions verified working in development. Upper bounds
# are set only where a major version is known to break the API (numpy).
# =============================================================================

# --- Core numerical stack ---------------------------------------------------
numpy>=1.24.0,<3.0
scipy>=1.10.0

# --- Reinforcement learning -------------------------------------------------
# Gymnasium is the modern fork of OpenAI Gym; SB3 needs the v1.x API.
gymnasium>=1.0.0
# Stable-Baselines3 brings PyTorch in as a hard dependency, so PyTorch will be
# auto-installed if not already present; we pin a recent version explicitly
# so the resolver does not pick something older than the project was tested on.
stable-baselines3>=2.0.0
torch>=2.0.0

# --- Visualization (required by GUIs and validator plot outputs) ------------
matplotlib>=3.8.0
pillow>=10.0.0

# --- PDF / report generation ------------------------------------------------
# Used by every script under docs/ to produce the Fischer Implementation
# Report, the Reward System Specification, the academic reference report,
# and the CP493 progress report. Optional if the professor does not need to
# regenerate the PDFs (they are already shipped under docs/).
reportlab>=4.0.0

# --- Data tools (used by some legacy example notebooks and saved-history CSVs)
pandas>=2.0.0

# =============================================================================
# OPTIONAL DEPENDENCIES — uncomment if the corresponding feature is needed
# =============================================================================
#
# Jupyter / notebook exploration of training data:
# jupyter>=1.0.0
# notebook>=7.0.0
# ipython>=8.15.0
#
# Extra plotting libraries used by legacy example dashboards:
# seaborn>=0.13.0
# plotly>=5.17.0
#
# SB3 TensorBoard logging (training emits TB scalars if tensorboard is on path):
# tensorboard>=2.13.0
#
# Older sklearn examples in the data/ subpackage:
# scikit-learn>=1.3.0
#
# Pygame for enhanced rendering on the arm-control GUI (optional alternative
# backend; default Matplotlib backend works without it):
# pygame>=2.1.0
#
# =============================================================================
# OS-LEVEL DEPENDENCIES (NOT pip-installable) — install via system package mgr
# =============================================================================
#
# The training and arm-control GUIs use Tkinter. Tkinter is bundled with the
# official python.org installer on Windows and macOS and with Homebrew Python
# on macOS, but on most Linux distributions it must be installed separately:
#
#   Ubuntu / Debian:    sudo apt install python3-tk
#   Fedora / RHEL:      sudo dnf install python3-tkinter
#   Arch:               sudo pacman -S tk
#
# To regenerate the slide deck (docs/Fischer_Implementation_Presentation.pptx)
# from source, install LibreOffice (only needed for editing the pptx; the
# existing file opens in PowerPoint, Keynote, or Google Slides):
#
#   macOS:              brew install --cask libreoffice
#   Ubuntu / Debian:    sudo apt install libreoffice
#   Windows:            https://www.libreoffice.org/download/
# =============================================================================
